if __name__ == '__main__':
    n = int(input().strip())
    dec=str(bin(n))
    len,max=0,0
    for i in dec:
        if(i=='1'):
            len+=1
            if(len>max):
                max=len
        else:
            len=0
print(max)
